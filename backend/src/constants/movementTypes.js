/**
 * CERADRIVE ERP — Movement Type Constants (Backend)
 * Re-exports from @ceradrive/shared/movementTypes — single source of truth.
 *
 * Codes are verified against live movement_types table in Part 3 DB audit.
 */
export { MOVEMENT_TYPES } from '@ceradrive/shared/movementTypes';
